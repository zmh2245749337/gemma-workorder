[project]
name = "gemma-tool-use"
version = "0.5.0"
description = "Agent tool-use post-training, guarded runtime and scenario evaluation for Gemma 3-1B."
requires-python = ">=3.10"
dependencies = [
  "torch>=2.4",
  "transformers>=4.50.0,<6",
  "accelerate>=1.0",
  "bitsandbytes>=0.45",
  "huggingface-hub>=0.27",
  "peft>=0.14",
  "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
